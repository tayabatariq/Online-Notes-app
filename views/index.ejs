<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Document</title>
    <script src="https://cdn.tailwindcss.com"></script>
  </head>
  <body>
    <div class="main w-full min-h-screen bg-zinc-900">
      <div class="form p-10">
        <form action="/create" method="post">
          <input name="title"
            class="block w-full rounded-lg outline-none text-white px-5 py-2 bg-zinc-800"
            type="text"
          />
          <textarea

            class="block px-5 py-2 resize-none mt-2 w-full rounded-lg outline-none text-white bg-zinc-800"
            name="details"
            id=""
          ></textarea>
          <input
            class="mt-2 bg-blue-700 rounded-md px-5 py-5"
            type="submit"
            value="Create Task"
          />
        </form>
      </div>
      <div class="tasks flex flex-wrap gap-2 p-10">

        <% if(files.length > 0){%>
       <%  files.forEach(function(val){%>
        <div class="task min-w-72 rounded-md px-5  py-4 bg-zinc-800">
          <h1 class="text-white text-[1.2rem] ">
           <%= val %>
          </h1>
          <div class="flex w-full justify-between items-center mt-4">
            <a href="/files/<%= val %>" class="text-blue-800 text-[1rem] inline-block ">Read More</a>
          <a href="/edit/ <%= val %>"  class="text-[.9rem] hover:text-blue-800 text-gray-700">Edit file name</a>
          </div>

        </div>
      <% })%>

       <% }else{%>
        <h1 class="text-zinc-500 ">Not task yet!</h1>


       <% } %>
       
      </div>
    </div>
  </body>
</html>
